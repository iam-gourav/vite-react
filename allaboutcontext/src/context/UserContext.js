// here we import createContext

import { createContext } from "react";

// we decleare UserConxt with empty value
// we can also pass default value in createContext
export const UserContext = createContext();